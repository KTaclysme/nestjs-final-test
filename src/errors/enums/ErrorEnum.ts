export enum ValidationErrorEnum {
    EMAIL = 'Validation isEmail on email failed',
    ID = 'Validation isInt on userId failed',
    UNIQUE = 'must be unique',
}
export enum ApiErrorEnum {
    USER_NULL = 'User is null',
}
